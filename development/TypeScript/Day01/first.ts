// to prevent common problems in JavaScript we can use TS.

// it always needs a comiler of typescript, first we need to download it
// how to download TS

// step 1 - 
// npm install -g typescript
// write sudo befor above command to run as admin in matchMediain windows run as adminitrator 
// sudo npm install -g typescript

// step 2 - check it is install or not
// tsc--version
// or
// tsc -v

let a :number  =10;
let b :string = "himanshu"

console.log(a)
console.log(typeof b)

let c = {a:10,
    b:"him"
}
let d = "hi"
console.log(d);

console.log(c);
console.log(typeof c);

