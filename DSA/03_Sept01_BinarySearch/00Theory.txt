what is binary search - binary search is searching a algorithm in alimited space

it uses divide & conqurer algorithm.
it works only in sorted array/lists.


algo - 

method -1 - using while loop

1- create 2 pointer for first and last index:- start = 0, end = n-1(where n is the length of an array)
2- find the mid of array - mid = end +(end-start)/2
3-  while(start<=end){
    if (arr[mid]==target){
        return mid; //return the index of target element
    }
    else if (target<arr[mid]){
        end = mid-1;
    }
    else{
        start = mid+1;
    }
    return -1; // if target not found it return target not present in an array
}


method 2 - using recursive algo;

decleare start, end in the main function 

if (start>end ){
    return -1;
}
else if (target<arr[mid]){
    return BS(arr , target , start ,  mid-1);
}
else{
    return BS(arr , target , mid+1 , end)
}



in both cases time complexity is same - log(n)- coz every time array element divided into half;